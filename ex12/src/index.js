function inverseWhile () {
    var fiveNumbers = 0;
    while(fiveNumbers < 5) {
        console.log(fiveNumbers);
        fiveNumbers--;
    }
}
console.log(inverseWhile());
module.exports = inverseWhile;
